# ⚡ Mayhem Extension - Quick Start (No Icons Needed!)

## ✅ Ready to Use - No Setup Required!

The extension works **without icons** - it uses SVG automatically.

## Install in 30 Seconds

1. Open Chrome → `chrome://extensions/`
2. Enable "Developer mode" (top right)
3. Click "Load unpacked"
4. Select the `extension` folder
5. **Done!** ✅

The extension will work immediately - no icons needed!

## 🤖 Automation Features

### Control from Chat/Commands

The extension can be controlled via commands:

```javascript
// Click elements
click button.buy-button

// Type text
type input.search "token name"

// Navigate
navigate https://pump.fun

// Extract data
extract .token-name textContent
```

### Self-Building

The extension can modify itself in real-time:

```javascript
// Inject new features
execute const script = document.createElement('script'); script.innerHTML = 'console.log("New feature!");'; document.head.appendChild(script);

// Update UI
execute document.querySelector('#mayhem-badge').innerHTML = 'Updated!';
```

## 🎯 What It Does

- ✅ Auto-detects tokens on any trading platform
- ✅ Shows analysis popup
- ✅ Quick links to all platforms
- ✅ Can be controlled via commands
- ✅ Self-modifying capabilities
- ✅ Works without icons!

## 📋 Usage

1. **Load extension** (see above)
2. **Visit any token page** (Pump.fun, DexScreener, etc.)
3. **Click extension icon** → See token info
4. **Control via commands** → Automate actions

## 🚀 That's It!

No icons needed. No setup. Just load and go!

The extension is **fully functional** and **self-building** right now.

---

**Ready to automate! 🎯**

